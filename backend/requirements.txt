# ---------------------------------------------------------------------------
# Runtime dependencies (install with: pip install -r requirements.txt)
# ---------------------------------------------------------------------------

# --- Core API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-multipart>=0.0.12,<1.0        # required for multipart/form-data (image uploads)

# --- Database (used from the Database Integration task onwards) ---
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0                   # PostgreSQL async driver

# ---------------------------------------------------------------------------
# ML stack — uncomment during the ML Integration task.
# Kept commented so `pip install -r requirements.txt` stays fast/light while
# the backend foundation is being developed.
# ---------------------------------------------------------------------------
# torch>=2.3
# torchvision>=0.18
# ultralytics>=8.2                   # YOLO
# opencv-python-headless>=4.10
# pillow>=10.4
# numpy>=1.26
# scikit-learn>=1.5