# ---------------------------------------------------------------------------
# Runtime dependencies — pip install -r requirements.txt
# ---------------------------------------------------------------------------

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-multipart>=0.0.12,<1.0        # required for multipart/form-data image uploads

# ---------------------------------------------------------------------------
# Added in later tasks (kept commented until actually used):
#   Database Integration : sqlalchemy[asyncio]>=2.0, asyncpg>=0.30
#   ML Integration       : torch, torchvision, ultralytics, opencv-python-headless, pillow, numpy, scikit-learn
#   Chatbot Integration  : langchain, langgraph, ollama
# ---------------------------------------------------------------------------
